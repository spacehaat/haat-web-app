server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  location /api/ {
    # Override at deploy time via reverse proxy, or set VITE_API_URL at build time.
    return 502;
  }
}
